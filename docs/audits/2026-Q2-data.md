# Data Integrity & Audit Trail (DATA) — 2026 Q2

**Audit code:** DATA
**Date:** 2026-05-06
**Auditor:** Claude Code (draft) — pending Kyle review
**Scope:** Sampled queries against the demo tenant (post-simulation): orphaned join rows, audit-log presence, status-timestamp invariants, FK constraint count.
**Commit at audit time:** `b189c7e`
**Previous audit:** first

## Summary

No orphaned `work_order_asset` rows. No completed WOs missing `completed_at`. No dispatched SRs missing `work_order_id`. 14,837 audit log rows accumulated across the 12-month simulation. 55 foreign-key constraints registered. Program-related checkpoints (CP-DATA-6, the `program_run_asset` orphan check) skipped — programs not built. **Pass.**

## Findings

| ID | Severity | Area | Finding | Evidence | Tracking |
|----|----------|------|---------|----------|----------|
| (none) | | | | | |

## Pass/fail by checkpoint

- [x] CP-DATA-1 — every domain table has `created_at`/`updated_at`/`deleted_at`. Enforced via `TimestampMixin` + `SoftDeleteMixin` on every domain model. Exception: `audit_log` (append-only, uses `occurred_at`) — explicitly noted in `CLAUDE.md`.
- [x] CP-DATA-2 — audit log captures mutations. 14,837 rows present after the 12-month simulation.
- [Sampled] CP-DATA-3 — soft delete cascades. Sample-tested via existing `tests/test_*.py` soft-delete cases.
- [Skipped] CP-DATA-4 — full reconstructibility test of 5 closed WOs from > 1 year ago. The simulation only goes back 12 months minus today; first audit is too soon to test. Defer to next quarter.
- [Skipped] CP-DATA-5 — task definition versioning. Only one version of any task currently active; nothing to compare. Defer until the first version bump lands.
- [Pending] CP-DATA-6 — orphaned `program_run_asset`. Programs feature not built.
- [x] CP-DATA-7 — orphaned `work_order_asset`. 0 rows.
- [x] CP-DATA-8 — JSONB fields validate against schemas. Sampled `attrs`, `task_data`, `procedure`, `triggers` against their Pydantic shapes via `tests/test_task_catalog.py`.
- [x] CP-DATA-9 — FK constraints present. 55 FOREIGN KEY constraints in `information_schema`.
- [Pending] CP-DATA-10 — `address_cached` populated within 24h. The geocode worker is wired (`app/services/geocode_worker.py`) but realistic measurement requires production usage.

## Bonus invariants checked

- Completed WOs have `completed_at` set: 0 violations
- Dispatched SRs carry a `work_order_id`: 0 violations
- audit_log rows: 14837

## Script outputs

```
$ uv run python -c '<sampled queries — see Notes for SQL>'

audit_log rows: 14837
orphaned work_order_asset rows: 0
completed WOs missing completed_at: 0
dispatched SRs without work_order_id: 0
foreign key constraints in DB: 55
```

## Notes

The simulator's invariant guarantees (status + timestamps + FK relationships) match the operational model. This audit is more meaningful once production data has accumulated for ≥ 1 year (CP-DATA-4 reconstructibility) and once task definitions have been versioned (CP-DATA-5). Both trigger automatic re-runs.

## Sign-off

Reviewed: pending
Findings filed as tracking issues: none
Release-blocking findings: none
Next audit due: 2026-Q3
