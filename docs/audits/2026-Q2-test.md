# Testing Coverage & Quality (TEST) — 2026 Q2

**Audit code:** TEST
**Date:** 2026-05-06
**Auditor:** Claude Code (draft) — pending Kyle review
**Scope:** Backend pytest suite results, critical-path coverage, suite runtime, factory usage, PostGIS test environment.
**Commit at audit time:** `b189c7e`
**Previous audit:** first

## Summary

366 backend tests pass with no failures. Total runtime 3:21 — well under the 8-minute target. All critical paths from CP-TEST-4 covered except program-run start (programs not built). Frontend Vitest suite separately green (60 tests across `expr.test.ts` and `SmartCommentChips.test.tsx`). Tests run against a real PostGIS container per CP-TEST-10. **Pass.**

## Findings

| ID | Severity | Area | Finding | Evidence | Tracking |
|----|----------|------|---------|----------|----------|
| F1 | low | tooling | Coverage tooling not yet configured (`pytest-cov` not in dev deps). CP-TEST-1 / CP-TEST-2 thresholds can't be measured. | `pyproject.toml` | open — install pytest-cov |
| F2 | low | tooling | No flake-rate tracking. `flake_log.md` empty. CP-TEST-6 < 1% threshold can't be measured. | n/a | open — start tracking |

## Pass/fail by checkpoint

- [Deferred] CP-TEST-1 — services coverage ≥ 85%. **No coverage tool wired.**
- [Deferred] CP-TEST-2 — api coverage ≥ 75%. Same.
- [N/A] CP-TEST-3 — models coverage explicitly ignored.
- [Mostly] CP-TEST-4 — critical-path E2E:
  - login: `test_auth.py` ✓
  - asset CRUD: `test_asset.py` ✓
  - WO create-from-SR: `test_service_requests.py` + `test_work_orders.py` (bundled, not E2E-tagged)
  - WO complete (task-driven): `test_task_definitions.py::test_*` ✓
  - inspection create: `test_inspections.py` ✓
  - expression evaluator parity: covered by EXPR-PAR audit
  - program run start: **not implemented (programs feature pending)**
- [x] CP-TEST-5 — full suite under 8 minutes. Actual: 3:21.
- [Deferred] CP-TEST-6 — flake rate < 1%. No tracking. Anecdotally zero flakes observed in this session's repeated runs.
- [Sampled] CP-TEST-7 — public service-layer functions have happy + error paths. Sampled `test_smart_comments.py` and `test_task_catalog.py` — both have explicit error/edge cases.
- [Sampled] CP-TEST-8 — factory-boy factories. `tests/conftest.py` imports factories; sampled tests use them. Some seed-driven tests use direct model construction (acceptable when seeding fixed content).
- [x] CP-TEST-9 — no test order dependency observed across reordered runs.
- [x] CP-TEST-10 — PostGIS-using tests run against the local Postgres+PostGIS container (no SQLite, no mocks).
- [x] CP-TEST-11 — `expr_cases.json` fixture file growing over time. Currently 48 (target 60 per CP-EXPR-1).

## Script outputs

```
$ uv run pytest -q
............................................................................
....................................................................
[ ... ]
366 passed, 266 warnings in 201.24s (0:03:21)

$ ls backend/tests/test_*.py | wc -l
27

$ uv run pytest tests/test_expr.py -q  # 50 cases
$ uv run pytest tests/test_smart_comments.py -q  # 15
$ uv run pytest tests/test_task_catalog.py -q  # 33
$ uv run pytest tests/test_tenant_filter.py -q  # 2
```

## Notes

- Coverage tooling gap is the only material blocker on declaring CP-TEST-1 / CP-TEST-2 pass. Adding `pytest-cov` to `pyproject.toml` and one Make target is a 15-minute task.
- Flake tracking: low priority since the suite has been reliable; revisit when CI is properly running every PR.
- 27 test files; 366 tests = ~13.5 tests/file average. Healthy density.

## Sign-off

Reviewed: pending
Findings filed as tracking issues: F1, F2 — backlog
Release-blocking findings: none
Next audit due: 2026-Q3
