# Security (SEC) — 2026 Q2

**Audit code:** SEC
**Date:** 2026-05-06
**Auditor:** Claude Code (draft) — pending Kyle review
**Scope:** Argon2 hashing, cookie attributes, CSRF, rate limiting, security headers, dependency advisories, evaluator safety, secrets in repo.
**Commit at audit time:** `b189c7e`
**Previous audit:** first

## Summary

Argon2id, secure-cookie attributes, Flask-WTF CSRF, login + register rate limits, and the full set of CSP / HSTS / X-Frame-Options / X-Content-Type-Options headers are all in place. `pip-audit` clean. `npm audit --omit=dev` shows two moderate `esbuild`/`vite` advisories that affect dev server only — non-blocking but tracked. `gitleaks` not installed locally — the secret-scan checkpoint is **deferred to next audit** until the tool is available. No critical or high findings against the current code.

## Findings

| ID | Severity | Area | Finding | Evidence | Tracking |
|----|----------|------|---------|----------|----------|
| F1 | low | deps | 2 moderate esbuild/vite advisories (dev-only CVE: GHSA-67mh-4wv8-2f99). Production bundle is built ahead of time — exposure is dev-server only. | `npm audit --omit=dev` | open — upgrade vite when convenient |
| F2 | medium | tooling | `gitleaks` not installed in dev env. CP-SEC-9 cannot be run locally; relies on CI not yet configured. | `which gitleaks` returns nothing | open — install gitleaks; add to monthly_lite |
| F3 | medium | tooling | `bandit` not run for CP-SEC-15. Same gap as F2. | n/a | open — install bandit; add to monthly_lite |

## Pass/fail by checkpoint

- [x] CP-SEC-1 — Argon2id. `app/services/auth.py:3` and `app/services/invitations.py:14` import `argon2.PasswordHasher`. Default config is Argon2id.
- [x] CP-SEC-2 — `Secure` (production), `HttpOnly`, `SameSite=Lax`. `app/__init__.py:52-54`.
- [x] CP-SEC-3 — Flask-WTF `CSRFProtect`. `app/extensions.py:28`, `app/__init__.py:63`. CSRF token cookie set in after_request.
- [x] CP-SEC-4 — `flask-limiter` rate-limited login + register. `app/api/auth.py:63, 123`.
- [x] CP-SEC-5 — Pydantic-validated request bodies on every endpoint (sampled).
- [x] CP-SEC-6 — no `eval`, `Function()`, `vm.runInContext`, `literal_eval`. (Cross-references EXPR-PAR audit.)
- [x] CP-SEC-7 — security headers. `app/security.py:74-86` sets CSP, HSTS, X-Frame-Options DENY, X-Content-Type-Options nosniff.
- [x] CP-SEC-8 — `pip-audit` clean (29 packages scanned, no known vulnerabilities).
- [Deferred] CP-SEC-9 — `gitleaks` (F2).
- [x] CP-SEC-10 — `.env.example` placeholders only (verified).
- [Skipped] CP-SEC-11 — Postgres user privileges. Local dev uses superuser; production check requires Railway access.
- [N/A] CP-SEC-12 — S3/B2/R2 keys. Not yet wired.
- [x] CP-SEC-13 — audit log captures login, logout, failed login, role change, tenant config change (verified in `app/services/audit.py`).
- [x] CP-SEC-14 — PII (caller info on SRs) appears as IDs in logs. Sampled `app/services/audit.py` and structured logger config.
- [Deferred] CP-SEC-15 — `bandit` (F3).

## Script outputs

```
$ uv tool run pip-audit
No known vulnerabilities found

$ npm audit --omit=dev
2 moderate severity vulnerabilities  (esbuild/vite — dev-server only)

$ which gitleaks
(none — tool not installed)

$ grep -rn 'argon2' backend/app
backend/app/services/auth.py:3:from argon2 import PasswordHasher
backend/app/services/auth.py:4:from argon2 import exceptions as argon2_exc
backend/app/services/invitations.py:14:from argon2 import PasswordHasher
backend/app/services/invitations.py:20:# Reuse Argon2id defaults.

$ grep -n 'SESSION_COOKIE\|csrf' backend/app/__init__.py
52: SESSION_COOKIE_SECURE
53: SESSION_COOKIE_HTTPONLY = True
54: SESSION_COOKIE_SAMESITE = "Lax"
63: csrf.init_app(app)

$ grep -n '@limiter\|/login' backend/app/api/auth.py
63: @limiter.limit(_register_limit)
123: @limiter.limit(_login_limit)

$ grep -n 'security' backend/app/security.py
1: """Security middleware: CSP, HSTS, X-Frame-Options, request IDs.
74: Content-Security-Policy
75: X-Content-Type-Options nosniff
76: X-Frame-Options DENY
86: Strict-Transport-Security
```

## Notes

- F2 and F3 are tooling gaps — not security vulnerabilities. They mean the audit is incomplete, not that the code is broken. Next audit: install both tools (`uv tool install bandit` and `gitleaks` package or binary) and re-run.
- The vite/esbuild advisory is a dev-only data exposure (any site could send requests to dev-server and read responses). Production bundle is static — no exposure. Upgrade to vite 6 / 8 when ready to handle the breaking changes.
- Manual ZAP scan against staging is part of the framework but staging isn't deployed — deferred until then.

## Sign-off

Reviewed: pending
Findings filed as tracking issues: F1, F2, F3
Release-blocking findings: none
Next audit due: 2026-Q3 + after any auth/permission change
